DROP TABLE LIBROS;

CREATE TABLE LIBROS(
TITULO VARCHAR2(20),
AUTOR VARCHAR2(20),
EDITORIAL VARCHAR2(30)
);

SELECT * FROM LIBROS;

INSERT INTO LIBROS (TITULO,AUTOR,EDITORIAL)
VALUES
('El Rey','Alexis','Picaso');

SELECT * FROM LIBROS;

